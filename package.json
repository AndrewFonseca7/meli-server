{
  "name": "meli-server",
  "version": "1.0.0",
  "description": "Servidor para prueba de Mercado Libre",
  "main": "index.js",
  "scripts": {
    "start": "cross-env PORT=8080 node server/server.js",
    "dev": "cross-env NODE_ENV=local PORT=8080 nodemon --watch server ./server/server.js",
    "test": "jest --verbose",
    "test:coverage": "jest --verbose --coverage",
    "test:watch": "jest --verbose --watchAll"
  },
  "author": "Andrew Fonseca",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "jest": "^26.6.3",
    "nodemon": "^2.0.7"
  }
}